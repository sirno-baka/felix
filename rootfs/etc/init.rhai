// Felix system startup script.
// PID 1 launches this through: /bin/rhai /etc/init.rhai

print("init.rhai: Rhai system runtime is ready");

// Put one-shot boot automation here. Examples:
// fs.create_dir("/var/lib/my-service");
// fs.write_text("/var/lib/my-service/booted", "yes\n");
// let status = run("/bin/selftest");
// if status != 0 { print(`selftest failed: ${status}`); }

0
